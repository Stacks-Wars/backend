# sw-domain

Shared domain types for **Stacks Wars** (users, lobbies, seasons, game metadata, wallet DTOs).

Game engines and `sw-plugin` depend on this crate. The HTTP server also uses it.

```toml
sw-domain = "1.0.0"
```
